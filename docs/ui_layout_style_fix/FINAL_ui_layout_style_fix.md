# FINAL：UI 布局与样式修复

## 总结

修复右侧工作区与底部日志重叠、下拉框过高/弹层显示不全、按钮无主次区分三类问题。方案落在 `QMainWindow` 角区契约与 `ApplicationStyle` QSS，轨迹页做最小控件标注。

## 关键决策

日志通栏（底角归属 BottomDock），侧栏不再与日志抢同一竖带。

## 验证建议

1. 打开「工作区 → 机器人 → 轨迹编辑」，确认应用/重置等完整可见
2. 拖高底部日志，确认侧栏缩短且可滚动，无压盖
3. 展开程序/组下拉，确认高度紧凑、列表可滚看完
4. 确认「应用」「生成」为蓝色主按钮
