# TODO：日志遮挡二次修复

| 项 | 说明 |
|----|------|
| 重编 | **Widget** + **AiWidget**（及依赖它们的 Host/Exe）后整窗最大化目视 |
| 日志通栏 | 当前日志只在中央列；若必须通栏到底，需先解决侧栏高度契约再谈 Bottom Dock |
| m_runDock | 成员仍保留为 `nullptr`，语言切换里有空指针判断，可后续删字段清理 |
