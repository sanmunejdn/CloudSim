# TODO — design.parts

1. Dock 规格确认：thread/length 下拉后再 execute  
2. 装配位姿 / 成组螺栓头+杆合并为单 Body（`profile_xyz_mm`）  
3. 真螺纹、真齿廓 `model_ref` 替换  
4. Host 注册 `design.calc` Domain，与 parts 串联（算完填齿轮 params）  
5. 扩展更多规格与紧固件族
