# FINAL — 网页端轨迹 UI 对齐

CAD/BREP 轨迹生成页已按桌面信息架构对齐：5 列特征表、中文策略名、schema 驱动参数面板（含参数面扫描全字段）、400ms 防抖自动离散、面/边索引删除、离散参数模板。

新增 `GET /api/trajectory/feature-schema`。Host/Gateway Debug+Release 已编过，前端已部署至 `bin\x64d\web` 与 `bin\x64\web`。
