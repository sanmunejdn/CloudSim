# ACCEPTANCE backend_dag_upgrade

## 实施结果

- 已完成 `BackendDataBase` 类型化组件容器接口。
- 已完成 `BackendDataManager` DAG 关系管理与查询接口。
- 已完成仓储层从 `backendParentId` 到 `listEdges` 的关系源迁移。
- 已完成工程文件 `edges` 序列化与旧 `parentId` 兼容读取。
- 已完成后端树 DAG 引用节点显示与删除路径的 DAG 遍历适配。

## 验收检查

- [x] 可注册/查询后端对象（原接口兼容）。
- [x] 可建立多父关系并阻断环。
- [x] 可查询父/子/祖先/后代/根节点。
- [x] 保存工程包含 `edges`。
- [x] 打开旧工程（仅 `parentId`）保持兼容。
- [x] 打开新工程（`edges`）可恢复关系。
- [x] 后端树可展示引用节点 `(ref)`。

## 质量检查

- 代码风格：遵循现有 C++ 风格与 RAII 用法。
- 诊断检查：已执行改动文件 lint，未发现新增错误。
- 回归风险：
  - OSG 层仍为单父内部结构，当前采用“首父映射”策略。
  - 删除语义为“删除选中节点及可达后代节点”。

## 建议验证步骤（手工）

1. 导入多个模型对象，建立多父关系（通过加载包含 `edges` 的工程）。
2. 检查树节点是否在第二父下显示 `(ref)`。
3. 切换可见性，确认后代显隐联动无死循环。
4. 保存工程后重新打开，确认关系保持一致。
