# TODO — 网页端 React 轨迹对齐

1. **手工点验**：本机 `CloudSimWeb.exe` 走一遍 ACCEPTANCE 建议步骤（坐标系 / 拾取高亮 / 应用复位）。
2. **可选**：应用后自动选中指令树第一条 LINE（fallback `refreshInstructionTreeAfterTrajectory` 完整行为）。
3. **可选**：轨迹编辑「工艺模板 → 填充」接 `/api/trajectory/recipe`（当前仅改本地 recipe 状态）。
4. **后续归档**：点验稳定后可将本目录迁入 `docs/_archive/`，并更新 `_archive/INDEX.md`。
