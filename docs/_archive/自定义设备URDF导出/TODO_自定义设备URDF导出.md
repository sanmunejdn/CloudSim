# TODO — 自定义设备导出 URDF

| 项 | 说明 |
|----|------|
| 手工回灌 | 按 ACCEPTANCE 步骤在 GUI 验证一次 |
| collision | 本期未写 `<collision>`，需要时可与 visual 同 mesh |
| 未 Apply 导出 | 组装对话框可改为导出前自动 Apply 画布 |
| 设备库扫描 | 若希望导出包出现在设备页网格，需拷入 `resource/models` 约定目录 |
