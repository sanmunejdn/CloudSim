# TODO — 网页端轨迹对等（后续）

1. Mesh 三角区域视口点选（对齐桌面 Mesh 会话）
2. ~~算子参数表单对接 `TrajectoryOpParamSchema`~~：**已完成**（`GET /api/trajectory/op-schema` + 编辑页动态表单）
3. ~~`POST /api/pick/hover` 高亮预览~~：**已完成**（`polylinesWorld` + Three.js 高亮折线）
4. React/Vite 正式壳迁入轨迹页
5. PathPlan 按 `sceneRootBackendId` 过滤列表
6. 特征目录叠加编号 overlay（可选）
