# TODO — design.calc

## 已完成

- Wave A/B/C 核心公式与单测  
- `to_feature_compose` 毛坯映射 + templates  
- 文档 CONSENSUS / DESIGN / ACCEPTANCE / CATALOG  

## 待办

1. **AI Domain `design.calc`**：口语 → CLI/库 → 确认 → `executeActionPlan`  
2. **装配位姿**：中心距 `A_mm` 写入两毛坯相对位姿（现仅 meta）  
3. **渐开线齿廓 Host API**（可选，替换 blank）  
4. Wave D：锥齿全程序、伺服选型全表（需重写求解器）  
5. 清理扫描缓存是否入库：`_sheet_scan.json` / `_focus_labels.json`（体积大，可 gitignore）
