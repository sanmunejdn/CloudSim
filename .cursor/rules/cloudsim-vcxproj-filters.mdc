---
description: VS 工程 .vcxproj.filters 按功能划分；新增源文件后必须自动同步筛选器
globs: "**/*.{vcxproj,vcxproj.filters,h,hpp,cpp,cxx,cc}"
alwaysApply: false
---

# CloudSim Visual Studio 筛选器

## 结构（强制）

每个产品工程的 `.vcxproj.filters`：

1. 顶层：`inc`（头）/ `src`（源 / Qt Moc）/ 必要时 `resource`、`ops`、`External`
2. 子层：**按功能**分桶（如 `inc\Instructions`、`src\Simulation`、`src\MainWindow`），不是按字母、不是单层堆平

磁盘已有子目录时优先镜像：`inc\Foo\`、`source\Foo\`、`ops\Name\`、`resource\`、`inc\Ai\` 等。

跨工程引用用独立顶层：`External\...`、`inc\HostRef`、`src\WidgetBorrowed`、`inc\PluginHost`（由脚本根据路径判定）。

编码：筛选器文件 **UTF-8 BOM + CRLF**。

## 新增 / 移动头文件或实现时（自动同步）

在对应 `.vcxproj` 登记 `ClInclude` / `ClCompile`（及 QtMoc 等）之后，**必须立刻**同步筛选器，禁止只改 vcxproj 留下孤儿或未入筛选器的文件：

```bash
# 推荐：只给新文件分桶，保留已有 Filter 与 GUID
python scripts/generate_vcxproj_filters.py --sync --project <工程名>

# 未指定 --project 时同步 sln 内全部产品工程
python scripts/generate_vcxproj_filters.py --sync
```

全量按功能重分类（会改已有分桶，改前确认）：

```bash
python scripts/generate_vcxproj_filters.py --full
# 或等价：tools/RegenerateProjectFilters.ps1
```

仅创建缺失的 `.filters` 文件：

```bash
python scripts/generate_vcxproj_filters.py --only-missing
```

## Agent 检查清单

- [ ] 新 `.h` / `.cpp` 已写入 `.vcxproj`
- [ ] 已执行 `--sync`（指定 `--project` 更佳）
- [ ] 打开 `.vcxproj.filters` 确认新文件落在合理功能子夹（`inc\<功能>` / `src\<功能>`），而非裸 `inc`/`src` 堆平（除非规则判为 Global/Other 且无更好桶）
- [ ] 未手改 `OutDir`；未把筛选器当源码格式化入口

权威说明：`docs/SOURCE_CONVENTIONS.md` §6、`docs/MODULE_DEVELOPER_GUIDES.md`「Visual Studio 工程过滤器」。
