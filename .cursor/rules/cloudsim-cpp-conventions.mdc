---
description: CloudSim C++/Qt/OSG 编码与导出约定
globs:
  - "**/*.h"
  - "**/*.hpp"
  - "**/*.cpp"
  - "**/*.cxx"
  - "**/*.cc"
alwaysApply: false
---

# C++ / Qt / OSG 约定

- **导出**: 对外 API 使用模块头里的宏（如 `DATA_EXPORT`、`WIDGET_EXPORT`）；实现库工程需定义对应的 `*_LIB`（或静态 `BUILD_STATIC`），与现有 `*_global.h` 保持一致。
- **头文件**: 统一 **`#ifndef/#define/#endif`** 守卫，宏名为 **`工程名_文件名_H`**（如 `DATA_MESHBOOLEAN_H`、`CLOUDSIMCORE_EVENTHUB_H`）；不要使用 `#pragma once`。先包含本模块 `*_global.h`（若需要导出/UTF-8 等），再标准库、Qt、OSG，最后项目内双引号头。源文件编码为 **UTF-8 with BOM + CRLF**。完整约定见 `docs/SOURCE_CONVENTIONS.md`。
- **注释**: 面向使用者的类/重要接口使用 **`///` 中文说明**（与 `PointCloudBackendData.h` 等一致）；实现细节用简短中文或英文均可，避免冗长。
- **Qt 与路径**: 区分 **UTF-8 字符串**（如 `std::string`/`QString::toUtf8()`）与 **本地路径编码**（如 `QFile::encodeName`、`std::ifstream` 打开路径）；文件 API 注释中已约定者保持一致。
- **异常与日志**: Widget 侧若使用 **`HPL_TRY` / `HPL_CATCH`**（见 `widget_global.h`），保持现有模式；不要静默吞掉异常。
- **第三方**: OSG 常用 `osg::ref_ptr`、节点层次；勿引入与 vcxproj 未配置的新第三方库，除非同时更新工程与说明依赖。
